import React from "react"
import { Routes, Route } from "react-router-dom"

export default function SubRoutes() {
  return <Routes>
    <Route path="/anim/:id" element={} />
    <Route path="/" element={<></>} />
  </Routes>
}